# Data

This directory holds the clean smoke datasets needed to reproduce the public
results.

Guidelines:

- keep the first dataset small and readable
- prefer hand-authored local fixtures over benchmark sprawl
- include only the task surfaces that are actually exercised by clean configs
