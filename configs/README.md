# Configs

This directory is for stable runnable configs only.

The first expected configs are:

- `qwen25_smoke/*.yaml`
- `service_demo/*.yaml`
- later: `qwen35_smoke_v1/*.yaml`

Do not copy research-only config variants here unless they are part of a
documented public result.
